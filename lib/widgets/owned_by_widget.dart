import 'package:flutter/material.dart';

class OwnedByWidget extends StatelessWidget {
  const OwnedByWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Column(
        children: [],
      ),
    );
  }
}
